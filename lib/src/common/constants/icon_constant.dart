import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class IconConstant {
  final iconAdd = const Icon(Icons.add_circle);
  final iconPeople = const Icon(Icons.people);
  final iconEdit = const Icon(Icons.edit);
  final iconDelete = const Icon(Icons.delete);
  final iconCamera = const Icon(Icons.camera);
  final iconGallery = const Icon(Icons.image);
  final iconSave = const Icon(Icons.save);

}